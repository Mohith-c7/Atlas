apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: faios
  namespace: faios
spec:
  tls:
    - hosts:
        - app.staging.example.com
        - api.staging.example.com
      secretName: faios-staging-tls
  rules:
    - host: app.staging.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: faios-web
                port:
                  name: http
    - host: api.staging.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: faios-business-api
                port:
                  name: http
