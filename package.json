{
  "name": "faios",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=11.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "dev": "turbo run dev --parallel",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "turbo run lint --concurrency=1",
    "docker:build:ai-orchestrator": "docker build -f infra/docker/ai-orchestrator.Dockerfile -t faios/ai-orchestrator:local .",
    "docker:build:business-api": "docker build -f infra/docker/business-api.Dockerfile -t faios/business-api:local .",
    "docker:build:web": "docker build -f infra/docker/web.Dockerfile -t faios/web:local .",
    "docker:build:workers": "docker build -f infra/docker/workers.Dockerfile -t faios/workers:local .",
    "docker:build": "pnpm docker:build:web && pnpm docker:build:business-api && pnpm docker:build:workers && pnpm docker:build:ai-orchestrator",
    "ops:failure:http": "tsx tools/ops/http-failure-probe.ts",
    "ops:load:http": "tsx tools/ops/http-load-smoke.ts",
    "ops:smoke:production": "tsx tools/ops/production-smoke.ts",
    "smoke:github:e2e": "tsx tools/e2e/github-command-smoke.ts",
    "test": "turbo run test",
    "test:integration:execution": "pnpm --filter @faios/workers test:integration",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@faios/eslint-config": "workspace:*",
    "@faios/prettier-config": "workspace:*",
    "@faios/typescript-config": "workspace:*",
    "prettier": "^3.3.3",
    "tsx": "^4.19.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  }
}
